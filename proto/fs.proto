//

option cc_generic_services = true;

message BSRequest {
    required bytes features  = 1;
};

//
message BSResponse {
    repeated uint64 faceid = 1;
    repeated uint32 owner  = 2;
};


service SearchService {
  rpc Search (BSRequest) returns (BSResponse);
}
